We found a blocking RPG combat lifecycle bug during real gameplay.

BUG:
I played through an RPG combat encounter and successfully won the fight. The game recognized that the enemies were defeated, but the turn system continued indefinitely. Turns kept advancing and nothing transitioned to an end-of-combat result, exploration, or the next appropriate game state.

This must be investigated and fixed before proceeding with M4/M5 work.

IMPORTANT:
Do NOT assume the bug is merely visual.
Trace the authoritative combat/session state from:
player action
→ turn resolution
→ enemy defeat
→ victory detection
→ encounter completion
→ combat cleanup
→ transition to the appropriate post-combat state.

The authoritative repository state is current github/main.

FIRST:
Inspect the current RPG combat implementation and determine exactly where the lifecycle breaks.

Look specifically for:
- combat victory detection
- defeat/all-enemies-dead checks
- turn advancement
- active combat/encounter state
- combat phase/state machines
- initiative/turn queues
- end-of-combat handlers
- exploration ↔ encounter transitions
- cleanup of defeated entities
- pending actions
- asynchronous turn-resolution promises
- conditions that can leave combat in an active state after victory
- UI state that may remain mounted after the authoritative combat state has ended

Do not patch around the UI symptom.

ROOT-CAUSE REQUIREMENT:
Identify the authoritative condition that should terminate combat and explain why the current implementation fails to transition after victory.

EXPECTED BEHAVIOR:

When the final hostile entity is defeated:

1. The rules engine detects that the encounter has reached a terminal victory state.
2. No further enemy/player turns are generated.
3. Any pending combat action resolves cleanly.
4. Combat state transitions exactly once into a completed/victory state.
5. Combat-specific turn/initiative state is cleaned up.
6. Defeated combat entities remain correctly represented according to the existing world/entity model.
7. The encounter result is recorded.
8. Control returns to the appropriate exploration/session state.
9. The player can continue the game normally.
10. No duplicate transition, duplicate rewards, duplicate entity registration, or stale combat state remains.

IMPORTANT:
Victory must be idempotent.

If multiple systems observe the final enemy dying, they must not be able to complete the same encounter twice.

Also handle the analogous defeat condition correctly if the existing architecture supports it:
- party defeated should terminate combat rather than continue generating turns.

DO NOT redesign the combat system.

Do NOT:
- redesign the turn system
- redesign combat rules
- redesign the parser
- implement M5 exploration/encounter architecture
- change WorldBounds
- change chunk streaming
- change entity persistence
- add new RPG mechanics
- add unrelated features
- rewrite the combat architecture unless the root cause genuinely requires a minimal correction

Prefer the smallest architectural fix that makes the existing combat lifecycle authoritative and deterministic.

TESTING:

Add regression tests that reproduce the exact bug.

At minimum test:

1. Player defeats the final enemy.
2. Victory is detected.
3. No additional turn is generated.
4. Combat reaches a terminal state.
5. Post-combat transition occurs exactly once.
6. The player can continue from the resulting state.
7. Repeated victory checks cannot trigger duplicate completion.
8. Rewards/results are not duplicated.
9. Defeated enemies do not remain active combatants.
10. Party defeat, if already supported, also terminates combat correctly.

Add an E2E regression test that actually plays through the relevant combat flow far enough to reproduce the original failure.

The test must verify that after the final enemy is defeated:
- the combat UI/state exits,
- the turn counter stops,
- no additional turn begins,
- the appropriate post-combat/exploration state is reached.

Do not make the test simply call an internal `endCombat()` function. It needs to exercise the real lifecycle.

REGRESSION REQUIREMENTS:

Run the complete existing suite.

Report actual totals rather than assuming the previous counts.

Must pass:
- all unit tests
- all E2E tests
- TypeScript typecheck
- production build
- relevant static/lint checks
- architectural/code review

CURRENT BASELINE:
- 632 unit tests
- 177 E2E tests

Those numbers may change as regression tests are added; report the actual final totals.

IMPORTANT SCOPE CHECK:

Before committing, inspect the diff carefully.

The fix should be surgical.

If fixing this exposes a broader architectural problem involving M5 exploration ↔ encounter transitions, document it, but do not silently expand this task into M5.

M4 WorldBounds and M5 exploration/encounter work should remain separate unless the existing combat lifecycle literally cannot be corrected without a prerequisite change.

WORKFLOW:

1. Inspect repository.
2. Reproduce/trace the bug.
3. Identify root cause.
4. Write a short implementation plan.
5. Implement the minimal fix.
6. Add regression tests.
7. Run the full validation suite.
8. Perform code/architecture review.
9. Report:
   - root cause
   - exact fix
   - files changed
   - tests added
   - actual unit/E2E totals
   - typecheck/build results
   - review findings
   - any remaining concerns

STOP BEFORE COMMITTING.

Do NOT commit or push.

Wait for my explicit approval after the validation report.

Proposed commit, if approved:

fix(tabletop): terminate RPG combat on victory