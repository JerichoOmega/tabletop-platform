Yes. The next step should be the large finite-world validation phase—before we introduce chunks or streaming.

Send this to Replit Agent:

Phase 3 — Large-Area World Validation

The Intelligent Tabletop has completed:

* Hardening: 8d86434
* Phase A: 8cfc623
* Phase B: 0eac85d
* Phase C: 68466b7
* Phase C concurrency hardening: 92c8e59

Current baseline:

* 155/155 unit tests passing
* 148/148 E2E tests passing
* TypeScript clean
* Production build clean

The authoritative architecture specification is:

docs/WORLD_SCALE_VIEWPORT.md

⸻

Objective

Implement the next phase defined by the current specification: large-area finite-world validation.

The purpose of this phase is to prove that the Phase A–C architecture actually works when the world is substantially larger than the fixed tabletop viewport.

This is an architectural validation phase.

We are deliberately introducing a large finite map before implementing chunk streaming or procedural world generation.

⸻

Critical architectural principle

The tabletop is a fixed-size presentation surface.

The world is larger than the surface.

Therefore:

WORLD
┌──────────────────────────────────────────────┐
│                                              │
│          ┌──────────────────────┐            │
│          │      TABLETOP        │            │
│          │      VIEWPORT        │            │
│          │                      │            │
│          └──────────────────────┘            │
│                                              │
└──────────────────────────────────────────────┘

The viewport moves through the world.

The world itself does not move.

⸻

1. Inspect the current specification first

Read the current docs/WORLD_SCALE_VIEWPORT.md completely.

Determine the exact requirements for the next large-area phase.

Do not invent a conflicting architecture.

If the specification’s phase numbering differs from the terminology in this prompt, follow the current specification’s actual requirements, but keep the implementation limited to the large finite-world phase described here.

⸻

2. Create a large finite test map

Introduce the large map required by the specification.

The target is approximately 40×40 tiles, unless the current specification defines a different exact size.

This map is a finite deterministic test environment.

It is NOT:

* a procedural world
* an infinite world
* a chunk-streamed world
* a WorldState implementation
* a production campaign map

It exists specifically to prove that the viewport architecture works when:

world dimensions > viewport dimensions

⸻

3. Preserve deterministic map geometry

The large map must use the existing authoritative TileQueryFn architecture.

Do not create a second geometry system for the large map.

GameState.tileQuery remains the rules-facing geometry interface.

The rules engine must not know whether it is operating on:

* an 8×6 map
* a 40×40 map
* a future chunked world

The only geometry interface available to rules should remain the authoritative tile query abstraction.

⸻

4. Make the viewport genuinely smaller than the world

This is the key test.

Configure the large-world encounter so:

tileW < world.width
tileH < world.height

The viewport must display only a portion of the world.

Do not increase the viewport to fit the whole map.

Do not scale the entire map down to fit the tabletop.

The tabletop remains physically/visually fixed.

⸻

5. Verify viewport origin movement

The large map must cause ViewportState.originWx/originWy to become meaningful.

Verify that when the followed actor moves through the world:

actor wx/wy changes
        ↓
viewport-follow policy
        ↓
originWx/originWy changes
        ↓
visible world region changes

The actor’s authoritative wx/wy must never be changed by viewport movement.

⸻

6. Verify world coordinates remain authoritative

This phase must explicitly prove that a tile’s viewport position does not become its world position.

For example, if:

viewport origin = (20, 15)

then the top-left visible tile should represent:

world = (20,15)
viewport = (0,0)

and the next tile should represent:

world = (21,15)
viewport = (1,0)

etc.

No gameplay system may use (0,0) as the actor’s location merely because the actor is rendered at the top-left of the viewport.

⸻

7. Verify movement on the large map

Test movement across the large world.

Specifically test:

* movement near the world origin
* movement through the center
* movement near the right edge
* movement near the bottom edge
* movement across viewport boundaries
* movement that causes horizontal viewport movement
* movement that causes vertical viewport movement
* movement that causes both simultaneously

Movement legality must remain determined by GameState.tileQuery.

Viewport movement must not alter movement legality.

⸻

8. Verify targeting and combat coordinates

Ensure attacks and abilities continue using authoritative world coordinates.

Test scenarios where:

* attacker and target are both visible
* attacker is near the viewport edge
* target is near the opposite viewport edge
* viewport moves while actors retain their world positions
* an actor remains at the same world coordinate while its viewport coordinate changes

The same pair of world coordinates must produce the same rules result regardless of viewport origin.

⸻

9. Verify token rendering

Token lookup must continue using:

token.world position
       ↓
wx / wy
       ↓
visible tile

Never use:

viewport array index

as the authoritative token position.

Test a token whose world coordinate remains constant while the viewport moves around it.

The token must appear on the correct visible tile whenever that tile is within the viewport.

⸻

10. Verify culling / visible-tile generation

getVisibleTiles() must generate only the viewport’s visible logical tiles.

It must not generate/render the entire 40×40 world as the tabletop DOM grid.

For a viewport of:

tileW × tileH

the rendered tile grid should contain exactly the appropriate visible dimensions, not:

world.width × world.height

unless the viewport is actually that large.

This is an important scalability invariant.

⸻

11. Do NOT implement chunking yet

This is extremely important.

Do not implement:

* chunk loading
* chunk unloading
* chunk streaming
* async tile queries
* procedural generation
* WorldModel
* WorldState
* persistence
* dirty chunks
* chunk residency
* infinite-world behavior

The large map is intentionally a single finite deterministic world.

We are proving the viewport architecture first.

⸻

12. Preserve the current small maps

Existing 8×6 encounters must continue behaving exactly as before.

Do not replace the current maps.

Add the large test environment alongside them.

Existing tests must continue to pass.

⸻

13. Test the dead-zone properly

The previous Phase C audit identified that the 8×6 maps make the Y-axis dead zone degenerate:

tileH = 6
2 × margin = 6

The large map must give the viewport enough room to test a genuine dead zone in both dimensions.

Add tests proving:

Inside dead zone

Actor moves:

viewport does not move

Cross horizontal boundary

viewport moves horizontally

Cross vertical boundary

viewport moves vertically

Cross both

viewport moves on both axes

Near world edges

Clamping prevents the viewport from exceeding:

0

and:

worldDimension - viewportDimension

⸻

14. Test coordinate invariants

Add explicit regression tests for:

worldToViewport(viewportToWorld(v)) === v

and:

viewportToWorld(worldToViewport(w)) === w

across multiple non-zero viewport origins.

Include coordinates from:

* origin
* center
* edges
* corners
* negative coordinates at the utility level where supported

⸻

15. Test viewport independence

Explicitly prove that changing:

originWx
originWy

does not change:

* GameState
* combatant wx/wy
* HP
* action state
* initiative
* turn order
* RNG
* legal movement
* legal attacks
* ability validation

The viewport must remain purely presentational.

⸻

16. Performance expectations

This phase is the first opportunity to verify that the tabletop does not render the entire world.

Measure or test:

rendered tile count

against:

viewport.tileW × viewport.tileH

Do not prematurely introduce sophisticated virtualization infrastructure if the current viewport grid already satisfies the requirement.

The goal is to establish the correct architecture, not optimize a problem that does not yet exist.

⸻

17. Asset handling

Reuse existing canonical assets wherever possible.

Do not generate new visual assets merely to populate the large test map.

If the large map needs additional terrain representation, use existing logical terrain/asset mechanisms or simple deterministic test geometry.

Do not redesign the asset registry.

Do not modify approved production assets.

⸻

18. E2E validation

Run the complete Playwright suite.

Existing behavior must remain green.

Add focused E2E coverage for the large-map viewport if practical and consistent with the existing testing architecture.

Verify:

* actor movement
* viewport movement
* targeting
* token visibility
* responsive rendering
* no regression to small-map encounters

⸻

19. TypeScript and build

Run:

* tsc --noEmit
* all Vitest tests
* all Playwright E2E tests
* production build

Do not weaken, skip, or delete existing tests.

⸻

20. Scope boundary

STOP after this large-area validation phase.

Do not automatically proceed to chunking or streaming.

Do not implement the next phase.

The purpose of this phase is to prove:

A fixed tabletop viewport can correctly navigate a deterministic finite world substantially larger than itself while the rules engine remains completely independent of the viewport.

⸻

Completion criteria

The phase is complete only when:

1. A deterministic large map exists.
2. The world is larger than the viewport.
3. The viewport displays only its visible region.
4. Non-zero viewport origins work correctly.
5. Actor movement can move the viewport.
6. Dead-zone behavior works in both axes.
7. World coordinates remain authoritative.
8. Token lookup remains world-coordinate based.
9. Movement/attack/ability rules remain viewport-independent.
10. Current 8×6 encounters remain unchanged.
11. No chunking or streaming has been introduced.
12. All unit tests pass.
13. All E2E tests pass.
14. TypeScript passes.
15. Production build passes.

⸻

Final report

Provide:

* commit hash
* files changed
* large-map dimensions
* viewport dimensions
* how the large map is represented
* how viewport/world mapping was verified
* tests added
* total unit tests
* total E2E tests
* TypeScript result
* production build result
* performance/rendered-tile observations
* confirmation that chunking/streaming was not implemented
* any remaining architectural risks
* whether the repository is ready for the next world-scale phase

This phase is about proving the architecture at scale, not introducing the next system. Stop when the phase is complete.