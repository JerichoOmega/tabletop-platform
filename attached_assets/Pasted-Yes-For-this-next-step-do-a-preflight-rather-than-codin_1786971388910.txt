Yes. For this next step, do a preflight rather than coding. Phase E is where the architecture becomes substantially more complex, so I want Replit to challenge the design before it touches the implementation.

Send Replit this:

Phase E Preflight — Chunking, Residency & Streaming Architecture

The Intelligent Tabletop has completed the large-area finite-world validation phase at commit 73e7f3d.

Current baseline:

* Hardening: 8d86434
* Phase A: 8cfc623
* Phase B: 0eac85d
* Phase C: 68466b7
* Phase C hardening: 92c8e59
* Phase D: 73e7f3d

Current verification:

* 217/217 unit tests
* 154/154 E2E tests
* TypeScript clean
* Production build clean

The authoritative architecture specification is:

docs/WORLD_SCALE_VIEWPORT.md

⸻

IMPORTANT: READ-ONLY PREFLIGHT

Do not modify any files.

Do not implement Phase E.

Do not create chunks yet.

Do not modify the current MapDef implementation.

Your task is to perform a deep architectural preflight for the next phase: chunking, residency, and streaming.

We have now proven that:

small finite world
        ↓
viewport

and:

large finite world
        ↓
viewport

work correctly.

Phase E must establish:

potentially large world
        ↓
chunk store
        ↓
resident geometry
        ↓
stable TileQueryFn
        ↓
rules engine

without breaking any of the deterministic simulation guarantees established in Phases A–D.

⸻

1. Read the architecture completely

Before producing the report, inspect:

* docs/WORLD_SCALE_VIEWPORT.md
* src/engine/content.ts
* src/engine/rules.ts
* src/engine/viewport.ts
* src/engine/intent*
* src/IntelligentTabletop.tsx
* state/clone/revalidation/execution code
* existing tests
* content validation
* any existing architecture documentation

Trace the actual implementation rather than relying only on the specification.

The goal is to determine whether the current architecture can safely evolve from:

MapDef → TileQueryFn

into:

WorldModel / ChunkStore → stable TileQueryFn

⸻

2. Define the chunk model

Determine exactly what a chunk should represent.

Resolve:

* chunk width
* chunk height
* chunk coordinate system
* chunk identity
* world-coordinate origin of a chunk
* conversion:

world coordinate ↔ chunk coordinate + local coordinate

* handling of negative world coordinates
* boundary behavior
* deterministic chunk keys

Do not simply assume that JavaScript % behavior is sufficient for negative coordinates.

Explicitly define mathematical floor-division/modulo behavior if negative world coordinates are supported.

⸻

3. Define chunk contents

Determine what belongs inside a chunk.

Consider separately:

Static geometry

* terrain
* walls
* pillars
* doors
* other blocking/LOS/cover information

Dynamic world state

* entities
* opened doors
* destroyed objects
* loot
* persistent mutations

Metadata

* chunk coordinate
* generation seed/version
* dirty state
* residency state

Do not collapse all of these into one structure unless the architecture genuinely requires it.

⸻

4. Resolve deterministic generation

Determine how a chunk can be generated deterministically from stable inputs.

Establish the conceptual contract:

world seed
+ chunk coordinate
+ generation version
      ↓
deterministic chunk

The same inputs must always produce the same static geometry.

Chunk generation must not depend on:

* viewport position
* render order
* React timing
* network timing
* current RNG state
* which chunks happened to be generated first

If the existing project has no world seed yet, determine the smallest safe abstraction to introduce later.

Do not implement it now.

⸻

5. Resolve chunk residency

Define the lifecycle precisely.

For example, the specification currently discusses concepts such as:

UNLOADED
LOADING
RESIDENT
DIRTY

Determine whether this is sufficient or whether another state is required.

Explicitly define:

* who requests a chunk
* who loads/generates it
* who marks it resident
* who marks it dirty
* who may evict it
* what happens during eviction
* when persistence occurs

Do not allow the renderer to become the authority over chunk residency.

⸻

6. Critical: define the rules-engine contract

This is the most important section.

The rules engine currently depends on:

GameState.tileQuery

and this was deliberately designed to be deterministic and stable.

Determine exactly how that remains true when chunks become asynchronous.

The rules engine must not do:

rules
  ↓
await chunk

and must not depend on:

"whatever chunks happen to be loaded right now"

Determine the correct architecture for:

ChunkStore
     ↓
stable geometry snapshot/view
     ↓
TileQueryFn
     ↓
GameState
     ↓
Rules

Resolve whether the TileQueryFn should:

* reference an immutable snapshot
* reference an immutable resident-set snapshot
* use a dedicated WorldGeometrySnapshot
* or use another architecture

Give a concrete recommendation.

⸻

7. Define behavior for unloaded chunks

This must be explicit.

What does:

tileQuery(wx, wy)

return if (wx, wy) belongs to an unloaded chunk?

Options might include:

* guaranteed preloading before rules evaluation
* a distinct unavailable result
* treating it as impassable
* throwing an error
* another mechanism

Do not choose based on convenience.

Consider movement, LOS, targeting, AI, proposal validation, and execution.

The architecture must prevent a transient loading state from silently changing gameplay semantics.

⸻

8. Define encounter residency guarantees

This is critical for tactical combat.

Determine how the system guarantees that all chunks needed by an active encounter remain resident.

For example:

encounter starts
      ↓
determine required world region
      ↓
ensure required chunks resident
      ↓
create stable encounter geometry
      ↓
GameState

During the encounter:

* chunks containing participants must remain valid
* rules cannot suddenly lose geometry
* proposal validation and execution must see consistent geometry
* viewport movement must not invalidate simulation data

Determine exactly where this guarantee belongs.

⸻

9. Define the relationship between WorldState and chunks

We previously established:

WorldState owns persistent world truth.

GameState owns active tactical truth.

endEncounter() is the controlled commit boundary.

Determine how chunks fit into that model.

Specifically:

WorldState
    ↕
ChunkStore
    ↓
WorldEntity
    ↓
buildEncounter()
    ↓
GameState

Determine whether WorldState:

* owns the ChunkStore
* references the ChunkStore
* is itself represented by chunks
* or uses another abstraction

Do not allow chunk residency to become a second source of truth for persistent entities.

⸻

10. Entity identity and chunk movement

Verify how the existing:

id
defId
worldId

model works when an entity crosses chunk boundaries.

A world entity moving from:

chunk (0,0)

to:

chunk (1,0)

must retain the same worldId.

Chunk eviction/reload must never create a new identity.

Determine what data is authoritative for entity location.

⸻

11. Dirty chunks and persistence

Determine what makes a chunk dirty.

Examples:

* entity moved
* entity died
* door opened
* object destroyed
* loot removed
* terrain permanently modified

Determine:

dirty
  ↓
persistence requirement
  ↓
safe eviction

Do not implement persistence yet.

We only need the architecture and contracts necessary to prevent future data loss.

⸻

12. Chunk boundaries must be invisible to gameplay

Establish this as a hard invariant:

A chunk boundary must never change gameplay semantics.

Crossing from one chunk to another must not alter:

* coordinate meaning
* movement rules
* LOS
* cover
* range
* entity identity
* RNG
* turn state

A tile at (31,10) must mean the same thing whether it is queried from the left chunk or the right chunk’s neighboring boundary logic.

⸻

13. Viewport relationship

Determine how viewport movement interacts with chunk residency.

The desired direction is:

viewport moves
     ↓
residency manager may prefetch chunks
     ↓
renderer gets available visible geometry

NOT:

viewport
     ↓
directly owns world truth

Determine:

* how many chunks should be prefetched
* whether a viewport may temporarily show an unloaded region
* whether the viewport waits for residency
* how loading states are represented

Do not implement visual loading behavior yet unless the current specification explicitly requires it.

⸻

14. Async vs synchronous boundaries

Explicitly document:

Async

* chunk loading
* chunk generation if implemented asynchronously
* persistence
* prefetch

Synchronous

* rules evaluation
* TileQueryFn
* movement validation
* LOS
* targeting
* proposal revalidation
* proposal execution

Resolve any contradiction between asynchronous world infrastructure and synchronous deterministic simulation.

⸻

15. RNG isolation

Verify that chunk generation cannot accidentally consume the same RNG stream used by gameplay.

Establish whether chunk generation requires:

* deterministic coordinate-derived seeds
* a separate generation RNG
* a separate deterministic PRNG
* another mechanism

Gameplay RNG and world-generation RNG must never accidentally share mutable state.

⸻

16. Performance architecture

Determine whether the current:

map.pillars.some(...)

linear lookup should be replaced before or during chunk implementation.

Compare:

* array
* Set
* coordinate-keyed map
* chunk-local lookup structures

Do not optimize prematurely, but identify the correct structure for chunk-local geometry.

⸻

17. Testing architecture

Define the tests Phase E will need.

At minimum include planned tests for:

Coordinate conversion

World → chunk/local and back again.

Include negative coordinates.

Deterministic generation

Same seed + same chunk coordinate → identical chunk.

Chunk isolation

Generating chunk A must not change chunk B.

Boundary correctness

Neighboring chunks agree at their shared boundary.

Residency

Load → resident → evict behavior.

Dirty state

Mutation → dirty → persistence requirement.

Entity identity

Entity crossing a chunk boundary retains worldId.

Rules determinism

Identical geometry snapshot produces identical rules results regardless of residency events.

Proposal atomicity

Revalidation and execution cannot observe different geometry snapshots.

Viewport independence

Moving the viewport cannot change simulation state.

⸻

18. Identify required changes to WORLD_SCALE_VIEWPORT.md

Compare the existing Phase E specification against your findings.

Identify every section that needs clarification or amendment before implementation.

Do not edit it.

Give exact recommended changes.

⸻

19. Identify implementation risks

Find anything that could cause expensive architectural rework if Phase E implementation begins immediately.

Prioritize:

* determinism
* persistence
* identity
* async/sync boundaries
* encounter safety
* chunk boundary correctness
* TileQuery stability
* WorldState ownership

⸻

20. Final verdict

Give exactly one of:

GO

Phase E specification is sufficiently precise and implementation can begin.

GO WITH CHANGES

Architecture is sound, but specific documentation/contract decisions must be resolved before implementation.

NO-GO

A fundamental architectural problem remains.

⸻

Required final report

Produce:

A. Executive Summary

Overall assessment.

B. Current Architecture

Explain how the current Phase A–D architecture actually works.

C. Proposed Chunk Architecture

Give concrete interfaces/data boundaries.

D. Async/Synchronous Boundary

Explicitly define what can be asynchronous and what must remain synchronous.

E. WorldState / ChunkStore / GameState Ownership

Give the exact authority model.

F. TileQuery Architecture

Explain exactly how GameState.tileQuery remains deterministic.

G. Encounter Residency Model

Explain how active encounters remain safe.

H. Chunk Coordinate Model

Include negative-coordinate behavior.

I. Required Specification Changes

List exact changes needed in WORLD_SCALE_VIEWPORT.md.

J. Implementation Plan

Break Phase E into safe implementation steps.

K. Risks

Rank P0–P3.

L. Final Verdict

GO / GO WITH CHANGES / NO-GO

⸻

Do not modify the repository.

Do not commit anything.

Do not implement Phase E.

The purpose of this task is to make sure we can implement chunking/streaming without compromising the deterministic rules architecture we’ve established through Phases A–D.

Stop after producing the report.