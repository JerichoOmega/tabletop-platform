Absolutely. Send Claude this exact Phase A verification prompt:

Phase A Verification — Intelligent Tabletop

Phase A is complete at commit 8cfc623.

Before we begin Phase B, perform a read-only verification audit of the Phase A implementation against the authoritative docs/WORLD_SCALE_VIEWPORT.md specification and the Phase 3 architectural decisions.

Do not modify any files. Do not refactor anything. Do not begin Phase B.

The purpose of this pass is to determine whether Phase A is architecturally correct, not merely whether the tests pass.

⸻

1. Coordinate Migration Audit

Search the entire repository for coordinate usage and verify:

* Combatant.x/y has been completely replaced by wx/wy wherever those values represent world/tile coordinates.
* No hidden local-coordinate assumptions remain.
* No UI code is treating wx/wy as pixel coordinates.
* No rules code is treating coordinates as viewport coordinates.
* Utility functions such as distance, movement, LOS, targeting, and occupancy operate correctly on wx/wy.
* Negative world coordinates remain mathematically valid.

Report every remaining x/y reference that is potentially relevant, even if it is legitimate. Categorize each as:

* Intentional/non-world coordinate
* Legacy coordinate usage
* Potential architectural problem

⸻

2. TileQuery Boundary Audit

Verify that GameState.tileQuery is now the authoritative geometry interface used by the rules engine.

Trace:

executeMove

reachableTiles

lineOfSight

validateAttack

validateAbility

and all related helpers.

Confirm that gameplay rules no longer depend directly on MapDef geometry except through the intended adapter/boundary.

Identify every remaining direct use of:

* isWall
* isPillar
* isBlocked
* map.width
* map.height
* map.entrance
* map.pillars

Determine whether each remaining use is legitimate or represents Phase A leakage.

⸻

3. TileQuery Immutability / Determinism Audit

This is especially important.

Inspect every place where TileQueryFn is created.

Verify that the current Phase A implementation cannot accidentally create a query function that depends on:

* mutable React state
* viewport state
* chunk loading
* rendering state
* asynchronous operations
* mutable caches
* external mutable objects

Confirm that the current adapter closes only over immutable/static map geometry.

Then verify that sharing tileQuery by reference in cloneState() is actually safe.

Explicitly answer:

Can two cloned GameStates ever observe different tile geometry through the same tileQuery reference?

If the answer is no, explain why from the implementation.

⸻

4. Revalidation / Execution Consistency

Trace the proposal system end-to-end:

proposal
→ revalidateProposal
→ executeProposalSteps
→ rules engine

Verify that revalidation and execution necessarily evaluate against equivalent geometry.

Look specifically for a situation where:

* revalidation sees one map/query
* execution sees another

or where a state transition replaces or invalidates tileQuery.

Also verify that the RNG atomicity work from the previous hardening pass remains intact after Phase A.

⸻

5. Cover / LOS Regression Audit

Verify the exact semantics of the new TileInfo contract:

passable
blocksLOS
providesCover

Confirm that:

Wall

* impassable
* blocks LOS
* preserves intended wall behavior

Pillar

* impassable
* does not block LOS
* provides cover

Floor

* passable
* does not block LOS
* does not provide cover

Make sure the existing combat rules still apply cover correctly.

⸻

6. Map Adapter Fidelity

Inspect mapDefToTileQuery().

Verify that it exactly reproduces the old Phase 2 MapDef semantics:

* current 8×6 map dimensions
* implicit border walls
* entrance opening
* pillar locations
* passability
* LOS
* cover

Compare the adapter logic against the pre-Phase-A behavior where necessary.

The adapter must not introduce any new gameplay interpretation.

⸻

7. Identity Model Audit

Verify that the three identity concepts remain distinct:

id
    encounter-local combatant identity
defId
    content/template identity
worldId
    persistent world identity

Confirm:

* existing fixtures can legitimately have worldId === undefined
* no code incorrectly assumes id === worldId
* no code uses defId as a persistent entity identifier
* the new field doesn’t accidentally change existing encounter behavior

Identify anything that Phase B–F could accidentally build on incorrectly.

⸻

8. State Ownership Audit

Verify that Phase A has not accidentally introduced WorldState concepts prematurely.

Confirm:

* GameState remains encounter/tactical state
* WorldState has not become an implicit dependency of the rules engine
* viewport state does not exist inside authoritative GameState
* rules remain independent of React/rendering

⸻

9. Test Quality Audit

Do not merely report that 107/107 unit tests and 148/148 E2E tests pass.

Determine whether the new Phase A tests actually protect the intended architectural invariants.

Specifically verify that tests protect:

* coordinate migration
* adapter fidelity
* pillar cover
* LOS
* deterministic tile querying
* optional world IDs
* proposal revalidation
* state cloning

Identify any important Phase A invariant that currently has no regression protection.

Do not add tests during this audit.

⸻

10. Phase B Readiness

Finally, determine whether Phase A is safe to build upon.

Give one of exactly three verdicts:

GO

Phase A is architecturally sound and Phase B can begin.

GO WITH CHANGES

Phase A is fundamentally sound but specific issues should be corrected before Phase B.

NO-GO

A foundational problem exists that makes Phase B unsafe to begin.

⸻

Required Report

Produce:

Executive Summary

Overall assessment of Phase A.

Confirmed Correct

Architectural decisions that are implemented correctly.

Findings

For every issue:

* file
* relevant function/component
* observed behavior
* why it matters
* severity
* recommended action

Categorize as:

* P0 — critical
* P1 — high
* P2 — medium
* P3 — low

Remaining Coordinate References

Give a complete categorized list of relevant remaining x/y usage.

Phase A Invariant Coverage

Table showing each Phase A architectural invariant and whether it is:

* enforced in code
* covered by tests
* documented

Phase B Risks

Specifically identify anything Phase B could accidentally make worse.

Final Verdict

GO / GO WITH CHANGES / NO-GO

Do not modify the repository.

Do not make speculative recommendations.

Do not recommend a rewrite.

Do not begin Phase B.

The goal is to prove that 8cfc623 is a safe architectural foundation for the viewport implementation.