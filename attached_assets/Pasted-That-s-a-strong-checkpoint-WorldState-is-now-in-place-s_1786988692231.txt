That’s a strong checkpoint. WorldState is now in place, so the architecture has reached the point where viewport streaming can safely attach to the world layer.

One thing I would specifically flag before the next prompt: the report says:

Combatant.id = Combatant.worldId = entity.worldId

That is not inherently wrong, but it weakens the three-layer identity distinction we deliberately established. The architecture says those identifiers have different semantic roles, even if they happen to share a value today. I would have the next Replit task audit and preserve the distinction, rather than blindly assuming the equality is permanent.

Current architecture

WorldState
├── ChunkStore
│    ├── async loading
│    ├── residency
│    └── immutable geometry snapshots
│
└── WorldEntityRegistry
     └── authoritative world entities
              │
              ▼
       beginEncounter()
              │
       pinned chunks
              │
       geometry snapshot
              │
              ▼
          GameState
              │
       synchronous rules
              │
              ▼
       endEncounter()
              │
              ▼
        WorldState commit

The next increment should therefore be viewport streaming integration, but still as a tightly bounded task.

Next prompt for Replit

Phase F — Viewport Streaming Integration

Phase F currently has:

* Phase E large-area validation complete
* Chunk foundation complete at 03af971
* Async chunk streaming complete at 932de54
* WorldState / WorldEntityRegistry complete at the current HEAD
* 440 unit tests passing
* TypeScript clean

The authoritative specification is:

artifacts/tabletop/docs/WORLD_SCALE_VIEWPORT.md

⸻

Objective

Implement the viewport streaming integration portion of Phase F.

This increment connects the already-existing:

ViewportState

to:

WorldState
  └── ChunkStore

so the viewport can asynchronously request nearby chunks while the active encounter continues using its immutable ResidentGeometrySnapshot.

This is an integration task, not a redesign of the viewport or rules engine.

⸻

Most important architectural invariant

There are now TWO different geometry concerns:

ACTIVE SIMULATION
-----------------
GameState.tileQuery
      ↓
ResidentGeometrySnapshot
      ↓
frozen at encounter start
PRESENTATION / VIEWPORT
----------------------
WorldState.ChunkStore
      ↓
async residency
      ↓
viewport-visible geometry

Never allow viewport streaming to mutate or replace GameState.tileQuery during an encounter.

This is the most important rule of this task.

A chunk loading because the camera moved must never change the geometry seen by the active rules engine.

⸻

1. Read the implementation before coding

Read:

* artifacts/tabletop/docs/WORLD_SCALE_VIEWPORT.md
* src/engine/viewport.ts
* src/engine/chunk.ts
* src/engine/world.ts
* src/engine/content.ts
* src/engine/rules.ts
* src/IntelligentTabletop.tsx
* all viewport tests
* all chunk tests
* all world tests

Trace:

newEncounter()
afterPlayerAction()
updateViewportForActor()
getVisibleTiles()
tileQuery
WorldState.beginEncounter()
WorldState.endEncounter()

Understand the existing coordinate transformations before changing anything.

⸻

2. Establish the viewport’s chunk requirements

Add a pure helper that determines which chunks are needed for the current viewport.

Conceptually:

ViewportState
     ↓
visible world rectangle
     ↓
intersecting chunk coordinates

Use the canonical Phase F coordinate functions.

Do not calculate chunk coordinates using:

Math.trunc()

or:

wx % CHUNK_W

Use the existing floor-based coordinate model.

Test:

* origin viewport
* non-zero viewport
* viewport crossing chunk boundary
* viewport spanning multiple chunks
* negative world coordinates if supported by the current world model
* exact chunk-boundary coordinates

⸻

3. Add viewport prefetch

Implement the documented prefetch policy.

The viewport should request:

visible chunks
+
prefetch margin

according to the Phase F specification.

Prefetch must be asynchronous and non-blocking.

Conceptually:

viewport changes
      ↓
calculate required/prefetch chunks
      ↓
ensureResident(...)
      ↓
continue rendering immediately

Do not await chunk loading from the React render path.

Do not block user input while adjacent chunks generate.

⸻

4. Do not replace the simulation snapshot

This is critical.

Suppose:

Encounter begins
     ↓
chunks A/B/C pinned
     ↓
snapshot created
     ↓
GameState.tileQuery created

Then the viewport moves:

viewport moves
     ↓
chunks D/E/F load

The existing:

GameState.tileQuery

must continue using the original snapshot.

It must not become:

tileQuery → WorldState.ChunkStore

and it must not be regenerated simply because the viewport moved.

⸻

5. Determine what the renderer queries

Carefully separate:

simulation geometry

from:

presentation geometry.

The renderer may use currently resident chunk geometry to determine what to display, but this must never become authoritative gameplay state.

If the viewport encounters a chunk that has not finished loading:

LOADING

render an explicit presentation-safe loading/placeholder state according to the specification.

Do not interpret a loading chunk as gameplay "void".

The rules engine’s "void" fallback remains a separate invariant-protection mechanism for missing snapshot geometry.

⸻

6. Introduce presentation loading state cleanly

If the current renderer needs a loading representation, keep it entirely outside GameState.

Suitable state includes:

ChunkResidency

or a presentation-level loading map.

Do not add:

loadingChunks

to GameState.

Do not add:

chunkStore

to GameState.

Do not add React state to the rules engine.

⸻

7. Preserve viewport/world coordinate authority

The existing architecture is:

VisibleTile
├── vx
├── vy
├── wx
└── wy

Keep wx/wy authoritative.

Token lookup must continue using world coordinates.

Tile clicks must continue translating:

viewport coordinate
      ↓
world coordinate
      ↓
rules engine

Never pass viewport-local coordinates into:

* executeMove
* validateMove
* validateAttack
* validateAbility
* proposal execution

⸻

8. Integrate viewport following with streaming

Existing:

updateViewportForActor()

should remain the authoritative camera-follow function.

After the viewport origin changes:

updateViewportForActor()
      ↓
new ViewportState
      ↓
determine required/prefetch chunks
      ↓
async ensureResident()

Do not embed chunk loading inside updateViewportForActor().

Keep it a pure function.

The viewport model should remain independently testable.

⸻

9. Avoid duplicate chunk requests

The existing ChunkStore.ensureResident() already deduplicates concurrent loads.

Reuse that behavior.

Do not build a second in-flight request cache inside the viewport system unless the existing API genuinely cannot support the requirement.

Multiple viewport updates may request the same chunk.

The ChunkStore should remain the authority for deduplication.

⸻

10. Encounter-required chunks vs viewport-prefetched chunks

Preserve the distinction:

PINNED

means required by the active encounter.

RESIDENT

may simply mean loaded for presentation/prefetch.

Viewport prefetch must not accidentally pin every visible chunk.

Otherwise moving the camera across a large world would permanently grow the encounter’s memory footprint.

Only the encounter’s required region is pinned.

⸻

11. Encounter lifecycle

Verify:

beginEncounter()
      ↓
required chunks pinned
      ↓
snapshot created
      ↓
viewport streaming active
      ↓
viewport may load additional RESIDENT chunks
      ↓
endEncounter()
      ↓
encounter-required chunks unpinned

Prefetched chunks may remain resident temporarily according to the ChunkStore lifecycle.

They must not become permanent pins merely because they were visible.

⸻

12. End-of-encounter cleanup

Ensure viewport streaming does not leave stale state after:

endEncounter()

and:

newEncounter()

The viewport should reset according to the existing initViewport() behavior.

Any old prefetch bookkeeping must not cause the new encounter to load chunks based on the previous encounter’s viewport.

⸻

13. React concurrency safety

Preserve the Phase C hardening:

setViewport(prev =>
  updateViewportForActor(prev, ...)
)

Do not reintroduce a stale viewport closure.

Any async chunk-loading callback must also avoid applying stale viewport state.

If a viewport moves:

A → B → C

and chunk A finishes loading after C, that completion must not reset the viewport to A or overwrite newer presentation state.

⸻

14. Async loading race safety

Test:

viewport A
    ↓
request chunks A
viewport B
    ↓
request chunks B
viewport C
    ↓
request chunks C
A finishes last

The final viewport must still be C.

Chunk A being resident is harmless.

What must never happen is:

A completion → stale viewport state

⸻

15. Rendering behavior

For a resident chunk:

render normally

For a loading chunk:

render presentation-safe loading state

For a chunk outside the viewport:

do not render it

For an encounter snapshot tile whose chunk is no longer live-resident:

GameState.tileQuery still works

because the snapshot owns the immutable geometry.

This last case must be explicitly tested.

⸻

16. No simulation dependency on viewport residency

Add a test demonstrating:

Same GameState
+
viewport moves
+
chunks load/evict
=
identical rules results

At minimum verify:

* reachable tiles
* line of sight
* attack validation
* movement validation

remain unchanged when only viewport residency changes.

This is one of the most important Phase F tests.

⸻

17. Preserve RNG isolation

Viewport movement and chunk prefetch must consume zero gameplay RNG.

Verify with the existing RNG test pattern:

draw
viewport movement
async chunk loads
draw

must equal:

draw
draw

⸻

18. Testing requirements

Add unit tests for:

Chunk selection

* viewport at origin
* non-zero origin
* chunk boundary
* multi-chunk viewport
* prefetch radius
* negative coordinates where supported

Streaming

* viewport change triggers loads
* duplicate requests are deduplicated
* loading is non-blocking
* failed load does not corrupt viewport

Rendering

* resident chunk renders normally
* loading chunk gets placeholder
* unloaded/outside viewport is not rendered

Encounter isolation

* pinned encounter chunks remain pinned
* viewport chunks remain merely resident
* viewport movement cannot change simulation snapshot
* evicting live chunks cannot invalidate active tileQuery

React/concurrency

* rapid viewport changes
* stale async completion
* encounter transition while loads are pending

RNG

* viewport streaming consumes no gameplay RNG

⸻

19. E2E testing

Add Playwright coverage for the actual 40×40 grandHall.

At minimum verify:

1. Only the visible viewport-sized set of tiles is rendered.
2. Moving/following the actor changes the visible world coordinates.
3. Tokens remain attached to their correct wx/wy.
4. A token crossing the viewport boundary appears correctly after scrolling.
5. A far-away token is not rendered.
6. Viewport movement does not alter gameplay state.

If the current UI does not expose enough interaction to drive these tests naturally, add the smallest deterministic test fixture/control necessary rather than weakening the architecture.

⸻

20. Preserve all existing functionality

Existing MapDef encounters must continue working.

Do not remove the current bounded-map adapter.

Do not rewrite the rules engine.

Do not rewrite the viewport module.

Do not remove the existing large-map tests.

⸻

21. Full regression suite

Run:

* TypeScript
* all Vitest tests
* all Playwright E2E tests
* production build

Current baseline:

440 unit tests
154 E2E tests

All must remain green.

Do not weaken, delete, or skip tests.

⸻

22. Scope boundary

This increment ends when viewport-driven asynchronous chunk streaming is working and tested.

Do not implement:

* persistence
* save/load
* database storage
* multiplayer
* infinite procedural campaign systems
* new combat mechanics
* new camera modes
* minimap redesign

Those are outside this task.

⸻

23. Important identity audit

During implementation, inspect the current WorldState mapping where the report states:

Combatant.id = Combatant.worldId = entity.worldId

Do not automatically change it, because existing tests may intentionally rely on the current behavior.

However, verify that the semantic distinction remains documented:

id     = encounter-local identity
worldId = persistent-world identity

If they currently share the same string value, document that this is an implementation convenience rather than making the two concepts interchangeable.

Do not perform a broad identity refactor unless the existing implementation violates the specification.

⸻

24. Final report

Report:

* commit hash
* files created/modified
* viewport → chunk selection algorithm
* prefetch policy
* loading-state representation
* renderer integration
* encounter pinning behavior
* simulation snapshot isolation
* stale async completion handling
* RNG isolation
* identity audit result
* new unit tests
* total unit tests
* new E2E tests
* total E2E tests
* TypeScript result
* production build result
* deviations from WORLD_SCALE_VIEWPORT.md
* remaining Phase F work

Stop after viewport streaming integration is complete. Do not proceed into persistence or unrelated systems without explicit authorization.