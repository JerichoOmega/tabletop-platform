That’s exactly the resolution we wanted. 9edf390 is a clean architectural checkpoint, and Phase F is now authorized to begin.

The phase naming correction is important too: we should now use the repository’s terminology going forward:

* Phase E = large-area 40×40 validation — complete at 73e7f3d
* Phase F = chunk/region streaming — now preflight-resolved at 9edf390

The critical architecture is now locked:

WorldState
    │
    ├── ChunkStore
    │      ├── live residency
    │      ├── dirty state
    │      └── chunk geometry
    │
    └── WorldEntityRegistry
              │
              ▼
       buildEncounter()
              │
       ensure + PIN chunks
              │
              ▼
   ResidentGeometrySnapshot
              │
              ▼
        TileQueryFn
              │
              ▼
          GameState
              │
              ▼
        Rules Engine

And importantly, nothing asynchronous crosses into the rules engine.

Next prompt for Replit

I would now have Replit implement Phase F incrementally, rather than giving it permission to build the entire streaming system in one shot.

The first implementation should establish the mathematical and data foundations, then stop.

Send this:

Phase F — Chunk/Region Streaming: Foundation Implementation

Phase F preflight and architectural resolution are complete at commit 9edf390.

The authoritative specification is:

artifacts/tabletop/docs/WORLD_SCALE_VIEWPORT.md

Read the Phase F sections completely before making changes.

Phase E (large-area 40×40 validation) is already complete at 73e7f3d.

Phase F is now the chunk/region streaming implementation phase.

⸻

Critical scope

Implement only the foundational portion of Phase F.

Do not attempt to implement the entire streaming system in one pass.

This first implementation increment must establish:

1. chunk coordinate mathematics
2. immutable chunk geometry representation
3. deterministic chunk generation
4. immutable ResidentGeometrySnapshot
5. snapshotToTileQuery()
6. initial ChunkStore residency lifecycle
7. comprehensive unit tests for all of the above

Do not yet implement asynchronous loading, persistence, viewport prefetch, or renderer loading states unless the existing Phase F specification explicitly requires them in this first increment.

Stop after the foundational implementation is verified.

⸻

1. Read before coding

Read:

* artifacts/tabletop/docs/WORLD_SCALE_VIEWPORT.md
* src/engine/content.ts
* src/engine/rules.ts
* src/engine/viewport.ts
* src/intent/parser.ts
* src/IntelligentTabletop.tsx
* encounter construction/building code
* cloneState
* proposal validation/revalidation/execution
* RNG implementation
* existing unit tests
* existing E2E tests

Follow the actual repository architecture.

Do not create parallel abstractions that duplicate existing functionality.

⸻

2. Chunk coordinate system

Implement the canonical coordinate conversion specified in Phase F.

Use:

cx = Math.floor(wx / CHUNK_W)
lx = wx - cx * CHUNK_W

and equivalent Y calculations.

With:

CHUNK_W = 16
CHUNK_H = 16

Local coordinates must always satisfy:

0 <= lx < 16
0 <= ly < 16

Support negative world coordinates correctly.

Do not use raw JavaScript % for local coordinate calculation.

Implement the inverse transformation as well.

Test at minimum:

(0,0)
(15,15)
(16,16)
(17,17)
(-1,-1)
(-16,-16)
(-17,-17)

and multiple cross-axis combinations.

Verify round trips:

world → chunk/local → world

exactly preserve the original coordinates.

⸻

3. Canonical chunk identity

Implement a deterministic chunk-key function.

It must uniquely identify:

(cx, cy)

including negative coordinates.

Use the format specified by the architecture unless the repository already contains a canonical equivalent that should be reused.

Do not create multiple competing chunk-key formats.

Add collision/uniqueness tests for representative positive and negative coordinates.

⸻

4. Immutable ChunkGeometryData

Implement the Phase F geometry representation.

Static chunk geometry must be immutable after generation.

It should support efficient lookup for:

* passability
* LOS blocking
* cover
* tile type where required

Use coordinate-keyed structures appropriate for O(1) lookup.

Do not use repeated Array.some() scans for chunk-local geometry.

Geometry must contain enough information for TileQueryFn to reproduce the existing TileInfo contract:

passable
blocksLOS
providesCover
type

Do not introduce a second incompatible tile-geometry model.

⸻

5. Deterministic chunk generation

Implement deterministic generation according to the Phase F specification.

The conceptual input is:

worldSeed
+ cx
+ cy
+ generationVersion

and the same inputs must always produce the same geometry.

Most importantly:

Do not use the gameplay RNG.

Do not consume:

* combat RNG
* initiative RNG
* enemy AI RNG
* proposal RNG

Chunk generation must have an independent deterministic random stream.

Verify:

generate(A)
generate(A)

produces identical geometry.

Also verify:

generate(A)
generate(B)
generate(A)

produces the same result for A as generating A alone.

This proves chunk generation order does not influence geometry.

⸻

6. ResidentGeometrySnapshot

Implement the immutable snapshot boundary defined in Decision 23.

The snapshot must contain copies of the geometry required by the encounter, not references to mutable live ChunkStore state.

The snapshot must not contain:

* React state
* viewport state
* live ChunkStore references
* mutable residency state
* loading promises
* eviction state

Its purpose is to provide deterministic geometry to the synchronous rules engine.

Make the immutability contract explicit in TypeScript and documentation/comments where appropriate.

⸻

7. snapshotToTileQuery()

Implement:

snapshotToTileQuery(snapshot): TileQueryFn

The resulting function must:

* be synchronous
* be pure
* close only over immutable snapshot data
* never access live ChunkStore state
* never await
* never inspect viewport state
* never consume RNG

For coordinates whose chunk is absent from the snapshot, return the canonical deterministic void result:

{
  passable: false,
  blocksLOS: true,
  providesCover: false,
  type: "void"
}

Do not silently treat this fallback as successful encounter residency. Missing required geometry remains an invariant violation during encounter setup.

⸻

8. Preserve the existing TileQuery contract

Compare the new snapshot-backed query against the existing:

mapDefToTileQuery()

for equivalent geometry.

Verify equivalent results for:

* floor
* wall
* pillar
* entrance
* void

Existing MapDef encounters must continue using their existing adapter unless the Phase F specification explicitly calls for migration in this increment.

Do not break the bounded-map path.

⸻

9. Initial ChunkStore

Implement the foundational in-memory ChunkStore specified by Phase F.

It must own live chunk lifecycle state.

At minimum support the architecture’s required concepts for:

UNLOADED
LOADING
RESIDENT
PINNED

and dirty state where specified.

The store must be the authority for live residency.

It must be possible to:

* generate/load a chunk
* mark it resident
* inspect residency
* pin it
* unpin it
* prevent eviction of pinned chunks
* create a snapshot from resident chunks

Do not allow GameState to receive the live store.

⸻

10. Snapshot barrier

Implement the architectural boundary:

ChunkStore
    ↓
required chunks resident
    ↓
freeze/copy snapshot
    ↓
snapshotToTileQuery()
    ↓
GameState

The snapshot must be stable after creation even if the live ChunkStore subsequently:

* loads another chunk
* evicts another chunk
* changes residency
* changes dirty state

Write tests that deliberately mutate live store state after snapshot creation and prove the existing snapshot-backed TileQuery result does not change.

⸻

11. Do NOT wire active encounters yet unless required by the specification

Do not redesign buildEncounter() prematurely.

If the Phase F specification explicitly requires the snapshot path to be introduced at encounter creation during this increment, implement the smallest adapter necessary.

Otherwise, keep the new infrastructure isolated and tested.

Do not remove the existing MapDef encounter path.

⸻

12. mapDefToTileQuery() pillar lookup

As part of this implementation, eliminate the repeated:

map.pillars.some(...)

lookup inside the TileQuery hot path.

Precompute a coordinate-keyed Set when creating the adapter, while preserving identical behavior.

Add regression tests proving the adapter’s output has not changed.

This is a data-structure correction required by the new geometry architecture, not a gameplay change.

⸻

13. Do NOT fix unrelated buildIntentContext() yet

The known:

state.map.pillars

leak in buildIntentContext() is documented.

Do not expand this foundational implementation into LLM integration work.

If the current Phase F specification explicitly requires this migration at this stage, perform only the narrowly specified change and add appropriate tests.

Otherwise defer it to the documented migration point.

⸻

14. Testing requirements

Add comprehensive unit coverage for:

Coordinate math

* positive coordinates
* zero
* chunk boundaries
* negative coordinates
* round trips

Chunk keys

* uniqueness
* negative coordinates

Generation

* deterministic output
* generation-order independence
* coordinate isolation
* version changes produce independent generation streams where specified

Geometry

* floor
* wall
* pillar
* void
* boundary coordinates

Snapshot

* immutable behavior
* missing chunk → void
* live store mutation cannot change snapshot results

Residency

* unloaded
* loading if implemented in this increment
* resident
* pinned
* pinned chunk cannot be evicted
* unpin permits normal lifecycle

RNG

Prove chunk generation does not alter the existing gameplay RNG sequence.

⸻

15. Existing regression suite

Run the complete existing suite after implementation:

* TypeScript
* all Vitest tests
* all Playwright E2E tests
* production build

Do not remove, weaken, or skip existing tests.

The existing 217 unit tests and 154 E2E tests must remain green.

⸻

16. No renderer/viewport streaming yet

Do not implement:

* viewport prefetch
* async renderer loading states
* visual chunk indicators
* chunk-based rendering
* dynamic camera behavior
* minimap changes

Those belong to later Phase F increments unless the current specification explicitly places them in this foundation step.

⸻

17. No persistence yet

Do not implement:

* IndexedDB
* localStorage persistence
* server persistence
* save files
* database schema

Dirty-state architecture may be established if required, but persistence itself is outside this increment.

⸻

18. No WorldState implementation yet

Do not implement the full:

WorldState
WorldEntityRegistry

system in this increment unless the specification explicitly requires it.

The architecture must remain compatible with those future systems.

⸻

19. Scope boundary

This is a foundation increment, not the entire Phase F.

Stop once:

* chunk coordinate mathematics works
* deterministic geometry exists
* snapshot architecture works
* TileQuery works from snapshots
* foundational residency works
* existing MapDef encounters remain intact
* tests are comprehensive and green

Do not automatically continue into asynchronous streaming.

⸻

20. Final report

Provide:

* commit hash
* files created
* files modified
* chunk dimensions
* coordinate model
* snapshot model
* residency lifecycle implemented
* RNG isolation mechanism
* whether buildEncounter() was changed
* whether existing MapDef encounters remain unchanged
* number of new unit tests
* total unit tests
* total E2E tests
* TypeScript result
* production build result
* any deviations from the Phase F specification
* remaining Phase F work

Most important: stop after this foundation increment. Do not begin asynchronous streaming, prefetch, persistence, or the next phase without explicit authorization.

This is the point where I’d favor small verified increments over a giant agent run. The snapshot boundary is the most important part of Phase F; if that comes out correctly, everything downstream has a solid place to attach.